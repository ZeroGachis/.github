name: 'Delete-Docker-Image'

on:
  workflow_call:
    inputs:
      image_version:
        required: true
        type: string
      image_name:
        required: false
        type: string
        default: ${{ github.event.repository.name }}


jobs:
  delete:
    runs-on: ubuntu-latest
    steps:
      - run: echo "disable deletion for this version on CI. Please upgrade to new one"
